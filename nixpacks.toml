# Railway nixpacks 构建配置(monorepo 模式)
# 默认 service 跑 apps/api/(后端)
# 前端单独 service 时用各自的 nixpacks 配置

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r apps/api/requirements.txt"]

[start]
cmd = "cd apps/api && uvicorn main:app --host 0.0.0.0 --port $PORT"
