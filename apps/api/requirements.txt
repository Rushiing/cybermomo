# Web 框架
fastapi==0.115.5
uvicorn[standard]==0.32.1

# 数据校验
pydantic==2.10.3
pydantic-settings==2.6.1

# 数据库
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6

# 认证
python-jose[cryptography]==3.3.0
authlib==1.3.2
httpx==0.28.1
bcrypt==4.2.0

# LLM
# 测试期统一走 百炼(DashScope)的 OpenAI-compatible 端点 → deepseek-v4-flash
openai==1.59.4
anthropic==0.40.0  # 备用(目前不用,后续可启用 Claude 路由)

# 工具
python-multipart==0.0.19
python-dotenv==1.0.1

# 错误监控(可选;SENTRY_DSN 没配置时不启用)
sentry-sdk[fastapi]==2.19.0
