# 会话交接 · 2026-06-26(内测前加固完成 + UX 打磨中)

> 给下一个会话(Claude / Codex / Rush)无缝衔接用。读这一份 + `beta-runbook.md` 就能接着干。
> 上一份基线是 AGENTS.md §2 的"当前节点",本文件是它 2026-06-26 的展开。

---

## 0 · 一句话现状

**内测前加固(5 P0 全闭环)已全部完成、推上线、部署验证通过**;现在在做"开真人内测前"的小 UX 打磨 + Rush 自己走 happy path 自测。下一步是开 ~100 人陆续进的真人内测。

- 仓库:`/Users/xihe/Documents/项目文件/cybermomo/app`
- 线上:`https://cybermomo-production.up.railway.app`(`/healthz` → `{"ok":true}` 已验)
- git:`origin/main = ee748f0`,工作区干净,**所有改动已推已部署**

---

## 1 · 这个会话做完的事(按时间)

### 内测前加固(Batch 1 → 3.5b)— 全部已合已部署

| Commit | 内容 |
|---|---|
| `38d356f` | **Batch1** 启动守卫(ENV=prod 配置不全即 crash)+ LLM 超时 60s/重试 2 + auth 全路径过滤 `deleted_at` + 删临时调试 endpoint |
| `49f2aab` | **Batch2 / P0-1 铁律3 确定性兜底**:profile domains/label 白名单 + `leak_scanner`(NFKC 归一抗绕过的 .md 泄露扫描)+ 剥 debug |
| `164eaaf` | **Batch1.5** 修 codex review 抓的 2 P0(启动守卫逻辑反了 / domains 非白名单)+ 3 P1 |
| `1371110` | **Batch3** pipeline 按 stage 补跑 endpoint(`/api/admin/pipeline/incomplete\|repair\|repair-all`)+ 内测 runbook |
| `d879a27` | **Batch3.5** summary per-host 幂等 + stale-running 僵尸回收 + `deleted_at` 登录/OAuth 闭环 + repair-all 进程级护栏 + leak_scanner 14 单测 |
| `51fe851` | **Batch3.5b** 修 codex 终审抓的 advisory lock 失效 P0:session 级锁在 `run_agent_chat` 内部 commit 后泄漏 → 改**专用连接持事务级 `pg_advisory_xact_lock`** + 2 P2 |
| `6f68e8d` | runbook nit |

**codex 终审两轮(都 `cx` 只读)最终判:可上线内测。** 5 个 P0 全代码闭环;最关键一条是 Batch3.5b——原来那把 advisory lock"看着像保护其实没锁住",codex 第一轮揪出来的。

### UX 打磨(本会话尾段)— 已合已部署

| Commit | 内容 |
|---|---|
| `c37fe5a` | **互聊回放内部信号人话化**:`AgentChatViewer.tsx` 里自己 Agent 的 `private_signals` 从生 JSON 换成友好胶囊(好感↑/聊得来劲/聊得比较深/触发底线…);`rewrite_level` 过程 meta 不展示;后端字段一个没动,只改前台呈现 |
| `ee748f0` | **个人房间决策点击提速**:`room/page.tsx` 里 `decide()` 原来 `await decision(~4s)` 后又 `await loadAll() 重拉整房间(~4s)` 串行 ~8.5s。改为:① 用 decision 返回值就地 patch 那张卡(删重拉)② 丢/再派 乐观更新(点下去秒翻"已决策",失败回滚)③ `loadAll({silent})` 让轮询/补刷不闪整页 loading |

---

## 2 · 档 B 延后清单(开放发布前必补,内测期靠规矩兜底)

都已写进 `beta-runbook.md`。内测期不阻断,但公开发布前要补代码闭环:

1. **平台底线封禁写入闭环(P0-2 / 铁律1)** — `user_hard_blocklist` 表是空壳,没代码写入。内测期靠手动 `UPDATE users SET deleted_at=now() WHERE id=X` 封人。
2. **后台 pipeline 连接池(P0-5)** — 后台 pipeline 无并发上限 + LLM 调用期持有 DB session;短时高并发可能撑爆池(4 worker × 10+10 = 80 < PG usable 97)。内测期靠**分批拉人**(30/批)mitigate。补法:全局 semaphore + LLM 期释放 session。
3. **repair vs 正常 pipeline 双建 chat** — 补跑的建 chat 锁只覆盖 repair 这条路,正常 onboarding pipeline 建 chat 没拿同一把锁。内测期靠"别在拉新开闸时跑 repair"规避。补法:normal pipeline / redispatch / repair **三处建 chat 入口统一收口到一个带锁的 create helper**。
4. DB job queue 替 BackgroundTask fire-and-forget;unified `summarize_for_llm()`。

---

## 3 · 已知小待办 / 下一步

- **UX 待办**:互聊回放里话题标签还显示 `matchpoint_3` 这种内部 id(jargon),待换人话 —— 上轮已 flag、**未做**,是下一个打磨候选。
- Rush 会陆续发更多小 UX 打磨项(短平快处理,可攒着一起 push)。
- **happy path 自测 → 开真人内测**(~100 人陆续进)。开闸前记 `beta-runbook.md §0` 三铁规矩:① 内测期不 deploy(后台 6-8min LLM 链路,重启会 kill 半截);② 封人靠 SQL;③ ADMIN_SECRET 已 rotate、别再贴聊天/截图。
- 最可能故障:"房间一直没简报" → `beta-runbook.md §2` 有 admin pipeline repair 一条龙。

---

## 4 · 协作 & 环境(衔接必知)

- **分工**:Claude 主修 / Codex review(`cx` 只读、直连 OpenAI 无代理);Mode B 自主多轮、轻量留痕。桥说明书:`~/Claude Code/2026-06-25-codex-bridge-gost.md`。
- **git Mode A**:Rush 自己 `git push origin main`(sandbox 挡 Claude 推送)。Claude 只本地 commit,给推送命令。
- **commit 约定 / 不要碰的区域 / 7 条平台铁律**:看 `AGENTS.md`(= `CLAUDE.md` 软链)。
- **本地测试局限**:本机无 pydantic / 无 web node_modules,跑不了全量测试;靠 `python3.11 -c ast.parse` 语法检查 + 纯函数 standalone 跑 + codex review。

### 关键文件指针
- `docs/audit-2026-06.md` — 双模型审计报告(5 P0 / 11 P1 / 8 P2,档 A 闭测 vs 档 B 公开发布分层)
- `docs/beta-runbook.md` — 内测 ops 手册 + 档 B 延后清单 + 故障应急
- `docs/codex-onboarding.md` — Codex 协作上手
- `docs/reviews/` — 逐 commit review 笔记
- `AGENTS.md` / `README.md` — 项目共识 + 本地启动
